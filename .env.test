# Test environment file deployed to the test server.
# This file is copied to .env on the remote host by scripts/redeploy.sh.

DATABASE_URL="postgresql://postgres:postgres@postgres:5432/pietrosoft_notes"
WORKSPACE_PATH=/data
BACKUP_DIR=/backups
BACKUP_HOST_DIR=/opt/bitacora-backups
NODE_ENV=production
APP_ENV=test
