services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - ./data:/data
    environment:
      - WORKSPACE_PATH=/data
      - NODE_ENV=production
    restart: unless-stopped
